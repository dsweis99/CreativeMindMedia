import { useEffect, useState } from 'react';
import { AnimatePresence, motion } from 'framer-motion';
import { CheckCircle2, X } from 'lucide-react';
import type { FeaturedOffer } from '../data/featuredOffer';

type FeaturedOfferModalProps = { isOpen: boolean; offer: FeaturedOffer; onClose: () => void };

export function FeaturedOfferModal({ isOpen, offer, onClose }: FeaturedOfferModalProps) {
  const [submitted, setSubmitted] = useState(false);
  useEffect(() => { if (!isOpen) setSubmitted(false); }, [isOpen]);

  return <AnimatePresence>{isOpen && <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-[80] flex items-end bg-black/70 p-0 sm:items-center sm:justify-center sm:p-6" onClick={onClose}><motion.section role="dialog" aria-modal="true" aria-labelledby="featured-offer-title" initial={{ y: 32, opacity: 0 }} animate={{ y: 0, opacity: 1 }} exit={{ y: 32, opacity: 0 }} className="relative max-h-[92vh] w-full overflow-y-auto bg-[var(--color-bg-primary)] sm:max-w-xl" onClick={(event) => event.stopPropagation()}><button type="button" onClick={onClose} className="absolute right-5 top-5 rounded-full border border-[var(--color-border-subtle)] p-2 text-[var(--color-text-muted)] transition-colors hover:text-[var(--color-accent)]" aria-label="Close offer form"><X size={20} /></button>{!submitted ? <div className="p-7 sm:p-10"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[var(--color-accent)]">{offer.campaignType}</p><h2 id="featured-offer-title" className="mt-3 pr-12 font-display text-5xl tracking-wide sm:text-6xl">{offer.buttonLabel}.</h2><p className="mt-4 max-w-md text-sm leading-relaxed text-[var(--color-text-muted)]">Tell us a little about your business. We’ll review your request and confirm the next step by email.</p><form className="mt-8 space-y-5" onSubmit={(event) => { event.preventDefault(); setSubmitted(true); }}><div className="grid gap-5 sm:grid-cols-2"><div><label htmlFor="offerName" className="mb-2 block text-sm font-medium">Name <span className="text-[var(--color-accent)]">*</span></label><input id="offerName" required placeholder="e.g. Jane Doe" className="w-full border border-[var(--color-border-subtle)] bg-[var(--color-card-bg)] px-4 py-3 text-sm outline-none transition-colors focus:border-[var(--color-accent)]" /></div><div><label htmlFor="offerBusiness" className="mb-2 block text-sm font-medium">Business name <span className="text-[var(--color-accent)]">*</span></label><input id="offerBusiness" required placeholder="e.g. Rose City Coffee" className="w-full border border-[var(--color-border-subtle)] bg-[var(--color-card-bg)] px-4 py-3 text-sm outline-none transition-colors focus:border-[var(--color-accent)]" /></div></div><div><label htmlFor="offerEmail" className="mb-2 block text-sm font-medium">Email address <span className="text-[var(--color-accent)]">*</span></label><input id="offerEmail" type="email" required placeholder="e.g. you@company.com" className="w-full border border-[var(--color-border-subtle)] bg-[var(--color-card-bg)] px-4 py-3 text-sm outline-none transition-colors focus:border-[var(--color-accent)]" /></div><div><label htmlFor="offerWebsite" className="mb-2 block text-sm font-medium">Instagram or website <span className="text-[var(--color-text-muted)]">(optional)</span></label><input id="offerWebsite" placeholder="e.g. @yourbusiness" className="w-full border border-[var(--color-border-subtle)] bg-[var(--color-card-bg)] px-4 py-3 text-sm outline-none transition-colors focus:border-[var(--color-accent)]" /></div><div><label htmlFor="offerDetails" className="mb-2 block text-sm font-medium">{offer.formPrompt} <span className="text-[var(--color-accent)]">*</span></label><textarea id="offerDetails" required rows={4} placeholder="Share a few details so we can give you a useful response." className="w-full resize-none border border-[var(--color-border-subtle)] bg-[var(--color-card-bg)] px-4 py-3 text-sm outline-none transition-colors focus:border-[var(--color-accent)]" /></div><button type="submit" className="w-full bg-[var(--color-accent)] px-5 py-4 text-sm font-semibold uppercase tracking-wider text-[var(--color-accent-fg)] transition-opacity hover:opacity-90">Send Request</button></form><p className="mt-4 text-xs leading-relaxed text-[var(--color-text-muted)]">No obligation. We’ll only use these details to respond to your request.</p></div> : <div className="p-10 text-center sm:p-14"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-[var(--color-accent-dim)] text-[var(--color-accent)]"><CheckCircle2 size={28} /></div><h2 className="mt-6 font-display text-5xl tracking-wide">Request received.</h2><p className="mx-auto mt-4 max-w-sm text-sm leading-relaxed text-[var(--color-text-muted)]">Thanks — we’ll review your request and get back to you by email.</p><button type="button" onClick={onClose} className="mt-8 border border-[var(--color-border-subtle)] px-5 py-3 text-sm font-semibold transition-colors hover:border-[var(--color-accent)] hover:text-[var(--color-accent)]">Close</button></div>}</motion.section></motion.div>}</AnimatePresence>;
}
